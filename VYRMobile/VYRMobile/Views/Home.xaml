<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:VYRMobile.ViewModels"
             mc:Ignorable="d"
             x:Class="VYRMobile.Home"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.HasBackButton="False">

    <ContentPage.BindingContext>
        <local:CronoViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <!--View del cronometro -->
        <StackLayout VerticalOptions="Start"
                    HeightRequest="300"
                        Margin="0,69,0,0">
            <!--<Frame  x:Name="CronometroView"
                        BackgroundColor="White"
                        CornerRadius="0"
                        WidthRequest="400"
                        HeightRequest="300"
                        VerticalOptions="Start"
                        HorizontalOptions="Center"
                        HasShadow="True">

                        <StackLayout>
                            <Label  Text="Tiempo transcurrido" 
                                    FontSize="18" 
                                    TextColor="Black" 
                                    HorizontalOptions="CenterAndExpand" 
                                    VerticalOptions="Center"/>

                            <Label  Grid.Column="0" 
                                    FontSize="40" 
                                    TextColor="#33B033" 
                                    Margin="20,0,0,0"
                                    HorizontalOptions="Start" 
                                    VerticalOptions="Center"
                                    WidthRequest="100">

                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding StopWatchHours, StringFormat='{0:D2}'}"/>
                                        <Span Text=":"/>
                                        <Span Text="{Binding StopWatchMinutes, StringFormat='{0:D2}'}"/>
                                        <Span Text=":"/>
                                        <Span Text="{Binding StopWatchSeconds, StringFormat='{0:D2}'}"/>
                                        <Span Text="."/>
                                    </FormattedString>
                                </Label.FormattedText>
                                
                            </Label>
                            <Label  Grid.Column="1" 
                                    FontSize="20" 
                                    TextColor="#33B033" 
                                    Margin="0,0,0,0"
                                    HorizontalOptions="Start" 
                                    VerticalOptions="Center">

                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding StopWatchMilliseconds, StringFormat='{0:D2}'}"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>-->
            <!--</StackLayout>
                    </Frame>-->
        </StackLayout>

        <StackLayout
                        VerticalOptions="Fill"
                        HeightRequest="500"
                        Grid.Row="2"
                        HorizontalOptions="Center">

            <Frame x:Name="MenuTareas"
                               BackgroundColor="White"
                               CornerRadius="5"
                               WidthRequest="250"
                               HorizontalOptions="Center"
                               HeightRequest="500">
                <Label Text="Tareas a solucionar:" />

            </Frame>

        </StackLayout>
        <!--<StackLayout
                        VerticalOptions="End"
                        HeightRequest="50"
                        Grid.Row="3"  >

                <Button     
                    x:Name="QR"
                    Command="{Binding ConnectCommand}"
                    Text="Scanear QR" 
                    TextColor="White"
                    BackgroundColor="#005EB2" 
                    CornerRadius="25" 
                    HeightRequest="50"
                    WidthRequest="175"           
                    HorizontalOptions="Center"
                    
                  />
                
            </StackLayout>
            <Button     
                    x:Name="alert"
                    Text="Alert" 
                    TextColor="White"
                    BackgroundColor="#005EB2" 
                    Margin="0, 0, 0, 40"
                    CornerRadius="25"
                    HeightRequest="50"
                    WidthRequest="125"           
                    HorizontalOptions="Center"
                    VerticalOptions="End"/>
            
            
            <Button     Grid.Row="2"         
                        x:Name="CallFrancisco"
                        ImageSource="call.png" BackgroundColor="#005EB2" BorderRadius="50"  HorizontalOptions="End" WidthRequest="60" HeightRequest="60" 
                        Margin="0,0,20,22" VerticalOptions="End"/>
            <Grid        
                Grid.Row="3"
                HorizontalOptions="End"
                Margin="0, 0, -24, 0">
                <controls:CallMenu 
                    SelectedCommand="{Binding ItemSelectedCommand}"
                    HorizontalOptions="Center" 
                    VerticalOptions="Center" />
            </Grid>
        </Grid>-->

    </StackLayout>
</ContentPage>